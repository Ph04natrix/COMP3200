import { Song } from "../../../types/audioResources"

export default function SongContextMenu(props: {
    selectedSong: Song
}) {
    // has a list of options


    return(<>
    {/* For each thing in the list return a button that does something */}    
    </>)

}